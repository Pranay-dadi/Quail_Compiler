# Quail_Compiler
FOR DEVELOPERS - INSTALL PACKAGES sudo apt update , sudo apt install llvm llvm-dev clang lld , sudo apt install cmake build-essential - CLONE REPOSITORY - GO TO build - DO rm -rf * - THEN cmake .. - THEN make - THEN ./Quail_Compiler ../test/sample.mc - SAMPLE TEST FILES IN test - THEN out.ll obtained - THEN llc out.ll -filetype=obj -o out.o - THEN clang out.o -o sample_exec - THEN ./sample_exec - THEN echo$? VIEWING RETURN VALUE OF OUTPUT - FOR UNOPTIMIZED LLVM CODE COMMENT cg.optimize IN main.cpp
